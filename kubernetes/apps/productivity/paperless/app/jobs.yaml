apiVersion: batch/v1
kind: CronJob
metadata:
  name: paperless-cronjob-fix-ownership
spec:
  schedule: "*/15 * * * *" # Every 15 minutes
  successfulJobsHistoryLimit: 1
  failedJobsHistoryLimit: 1
  jobTemplate:
    spec:
      ttlSecondsAfterFinished: 60
      template:
        spec:
          containers:
            - name: paperless-fix-ownership
              image: alpine/curl:8.12.1
              envFrom:
                - secretRef:
                    name: paperless-secret
              command:
                - sh
                - -c
                - |
                  echo "🔍 Installing jq..."
                  apk add --no-cache jq > /dev/null 2>&1

                  echo "🔍 Starting Paperless bulk permissions fix..."
                  PAPERLESS_URL="http://paperless-paperless-ngx.productivity.svc.cluster.local"
                  GROUP_ID=3  # Group ID for assignment
                  AUTH_HEADER="Authorization: Token $PAPERLESS_API_TOKEN"

                  # Function to update permissions using bulk API
                  update_bulk_permissions() {
                      ITEM_TYPE=$1  # Either "tags" or "correspondents"
                      echo "🔍 Checking for new $ITEM_TYPE..."

                      # Fetch items that are owned by the API user but not yet assigned to the group
                      ITEM_IDS=$(curl -s -H "$AUTH_HEADER" -H "Content-Type: application/json" "$PAPERLESS_URL/api/$ITEM_TYPE/" | jq -r '.results[] | select(.ownership != "group") | .id')

                      if [ -z "$ITEM_IDS" ]; then
                          echo "✅ No new $ITEM_TYPE found, exiting."
                          return
                      fi

                      # Convert list of IDs to JSON array
                      ID_LIST=$(echo "$ITEM_IDS" | jq -s '.')

                      echo "🔄 Bulk updating permissions for $ITEM_TYPE: $ID_LIST"

                      RESPONSE=$(curl -s -o response.json -w "%{http_code}" -X POST -H "$AUTH_HEADER" -H "Content-Type: application/json" \
                        "$PAPERLESS_URL/api/bulk_edit_objects/" -d "{
                          \"objects\": $ID_LIST,
                          \"object_type\": \"$ITEM_TYPE\",
                          \"operation\": \"set_permissions\",
                          \"permissions\": {
                              \"view\": { \"groups\": [$GROUP_ID] },
                              \"change\": { \"groups\": [$GROUP_ID] }
                          }
                        }")

                      if [ "$RESPONSE" -ne 200 ]; then
                          echo "❌ Error updating $ITEM_TYPE! HTTP Status: $RESPONSE"
                          cat response.json
                      else
                          echo "✅ Successfully updated permissions for $ITEM_TYPE"
                      fi
                  }

                  # Fix permissions for both tags and correspondents
                  update_bulk_permissions "tags"
                  update_bulk_permissions "correspondents"

                  echo "✅ Paperless bulk permissions fix completed!"
          restartPolicy: OnFailure
