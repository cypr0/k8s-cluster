apiVersion: batch/v1
kind: CronJob
metadata:
  name: paperless-cronjob-fix-ownership
spec:
  schedule: "*/15 * * * *" # Every 15 minutes
  successfulJobsHistoryLimit: 1
  failedJobsHistoryLimit: 1
  jobTemplate:
    spec:
      ttlSecondsAfterFinished: 60
      template:
        spec:
          containers:
            - name: paperless-fix-ownership
              image: alpine/curl:8.12.1
              envFrom:
                - secretRef:
                    name: paperless-secret
              command:
                - sh
                - -c
                - |
                  echo "🔍 Installing jq..."
                  apk add --no-cache jq > /dev/null 2>&1

                  echo "🔍 Starting Paperless tag and correspondent permissions fix..."
                  PAPERLESS_URL="http://paperless-paperless-ngx.productivity.svc.cluster.local"
                  GROUP_ID=3  # Group ID for assignment
                  AUTH_HEADER="Authorization: Token $PAPERLESS_API_TOKEN"

                  # Function to update ownership of an item (tag or correspondent)
                  update_ownership() {
                      ITEM_TYPE=$1  # Either "tags" or "correspondents"
                      echo "🔍 Checking for new $ITEM_TYPE..."

                      # Fetch items that are owned by the API user but not yet assigned to the group
                      NEW_ITEMS=$(curl -s -H "$AUTH_HEADER" -H "Content-Type: application/json" "$PAPERLESS_URL/api/$ITEM_TYPE/" | jq -r '.results[] | select(.ownership != "group") | .id')

                      if [ -z "$NEW_ITEMS" ]; then
                          echo "✅ No new $ITEM_TYPE found, exiting."
                          return
                      fi

                      # Assign new items to the group
                      for ITEM_ID in $NEW_ITEMS; do
                          echo "🔄 Updating permissions for $ITEM_TYPE ID $ITEM_ID..."
                          curl -X PATCH -H "$AUTH_HEADER" -H "Content-Type: application/json" "$PAPERLESS_URL/api/$ITEM_TYPE/$ITEM_ID/" -d "{\"ownership\": \"group\", \"group\": $GROUP_ID}"
                      done

                      echo "✅ $ITEM_TYPE permissions updated successfully!"
                  }

                  # Fix tags and correspondents
                  update_ownership "tags"
                  update_ownership "correspondents"

                  echo "✅ Paperless permissions fix completed!"
          restartPolicy: OnFailure
